Write a comprehensive test for: $ARGUMENTS

testing conventions:

- Use Vitests with React Testing Library
- robust and short test
- use loop where possible to make the test cases shorter with repeative test cases
- Place test files in a **test** dir in the same folder with the source
- Name test files as [filename].test.ts(x)
- Use @/prefix for imports

Notes: Focus on actual use cases rather than the implementations.
